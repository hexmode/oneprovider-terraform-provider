---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "oneprovider_image Resource - oneprovider"
subcategory: ""
description: |-
  OneProvider image resource (like AWS AMI).
---

# oneprovider_image (Resource)

OneProvider image resource (like AWS AMI).

## Example Usage

```terraform
# Create an image from an existing VM
resource "oneprovider_image" "example" {
  name  = "my-backup-image"
  vm_id = "12345"
}

# Use an image when creating a VM
# Note: for VM creation, template accepts either an OS template ID (string) or an image UUID.
resource "oneprovider_vm" "from_image" {
  hostname      = "restored.example.com"
  location_id   = 6
  instance_size = 108
  template      = oneprovider_image.example.id
}

# Import existing image:
# terraform import oneprovider_image.example image-id-123
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `vm_id` (String) Source VM ID to create image from

### Optional

- `name` (String) Image display name

### Read-Only

- `date` (String) Creation date
- `id` (String) Image ID
- `os_display` (String) OS display name
- `os_name` (String) OS name
- `size` (Number) Image size in GB
